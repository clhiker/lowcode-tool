TYPE_TABLE = """
页面模板, 表单模板, 图表模板, 主题模板, 组件模板, 菜单模板, 报表模板, 数据大屏模板, 列表模板, 地图模板, 向导模板, 搜索模板
业务流模板, 规则模板, 审批流模板, 任务模板, 事件模板, SLA模板, 跨应用编排模板
数据模板, 数据连接模板, 数据迁移模板, 导入导出模板, 测试数据模板
API模板, 集成模板, 消息模板, 通知模板, 单点登录模板, 支付模板
配置模板, 国际化模板, 审计日志模板, 日志模板, 异常处理模板, 环境配置模板, 定时任务模板, 脚手架模板
电商模板, CRM模板, IoT模板, 文件管理模板, 文档模板
"""

SYSTEM_PROMPT = f"""你是一位代码模板分类专家。

你需要为每个代码模板同时输出两个维度的分类：

【维度一：领域】
根据模板的**应用场景和所属行业**，按 GB/T 4754-2017《国民经济行业分类》的"小类"级别（4位代码+名称）输出**2~3个**领域标签，必须至少输出2个。
- **必须选择具体应用领域**（即代码实际服务落地的行业，如电商、金融、医疗、物流、教育、政务、制造业等），而非纯技术分类。
- 禁止使用"6513 应用软件开发"作为主分类。仅当所有描述完全通用、完全无法匹配任何具体行业时，才允许将它作为最后一个补充标签。
- 如果模板描述提到特定业务场景（如交易、捐赠、物流、登录等），必须优先归类到对应的具体行业代码。
- 严格使用 GB/T 4754-2017 中存在的真实代码和名称，不要编造。

【维度二：类型】
根据模板的技术形态和用途，从下方类别中选取最匹配的 1 个类型标签。

{TYPE_TABLE}

输出格式：
领域：<4位代码1> <名称1>, <4位代码2> <名称2>, ...
类型：<类型标签>

示例：
输入功能：交易信息表单
输入描述：该页面展示交易信息表单，用户可以填写姓名、金额等交易详情，选择支付卡，最终提交表单。
输出：
领域：5292 互联网零售, 6621 商业银行服务
类型：表单模板"""


def build_batch_prompt(items):
    lines = []
    for idx, (desc, func) in enumerate(items, 1):
        lines.append(f"<{idx}>\n功能：{func}\n描述：{desc}")
    batch = "\n\n".join(lines)
    return (
        f"以下是多个代码模板的功能和描述，请为**每一个**分别输出领域和类型分类。\n\n"
        f"{batch}\n\n"
        f"请严格按以下格式输出，每个模板对应一个块，领域必须输出2~3个：\n"
        f"<1>\n"
        f"领域：5292 互联网零售, 6621 商业银行服务, 6519 其他软件开发\n"
        f"类型：表单模板\n\n"
        f"<2>\n"
        f"领域：6420 互联网信息服务, 8394 教育辅助服务\n"
        f"类型：注册表单\n\n"
        f"严格使用 GB/T 4754-2017 中真实的4位代码和名称，不要编造。不要有多余文字。"
    )
